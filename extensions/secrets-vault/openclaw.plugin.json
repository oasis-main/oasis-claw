{
  "id": "secrets-vault",
  "name": "Secrets Vault",
  "description": "AES-256-GCM at-rest secrets store with deposit_secret tool and automatic redaction in session history. The agent receives an opaque handle; the plaintext only re-materializes inside tool calls that explicitly request it.",
  "contracts": {
    "_doc": "REQUIRED for agent-tool exposure (openclaw 2026.7.1 plugins/tools.ts:566-634): a plugin's registerTool'd tools materialize ONLY if declared here. Added 2026-08-04 (CLAW-076 find). Keep in sync with the api.registerTool names in index.ts.",
    "tools": [
      "deposit_secret"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "secretsDir": {
        "type": "string",
        "description": "Directory for the encrypted secrets vault (default: ~/.openclaw/state/secrets)"
      },
      "telegramBotToken": {
        "type": "string",
        "description": "Telegram bot token for deposit confirmations (optional)"
      },
      "telegramAlertChatId": {
        "type": "string",
        "description": "Telegram chat ID to confirm deposits to (optional)"
      }
    }
  }
}
