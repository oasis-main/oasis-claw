{
  "name": "@oasis-claw/oasis-reviewer",
  "version": "0.1.0",
  "description": "openclaw plugin: independent before_tool_call reviewer (two-layer policy — hard gitignore constraints + constitution). Shadow/audit skeleton.",
  "type": "module",
  "devDependencies": {
    "openclaw": "workspace:*",
    "vitest": "^3.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "zod": "^4.4.1"
  }
}
