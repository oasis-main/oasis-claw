{
  "id": "dot-swarm",
  "name": "Dot-Swarm",
  "description": "Shared .swarm/ stigmergy plugin. Injects .swarm/state.md + queue.md (and any configured peer files) into the agent's memory section via registerMemoryPromptSupplement (non-exclusive — coexists with the memory-core kind:\"memory\" backend, does not replace it). Also provides the swarm_read + compact agent tools and the swarm-compact CompactionProvider (handoff-driven context compaction backed by .swarm/state.md).",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "swarmDir": {
        "type": "string",
        "description": "Directory containing .swarm/ trail files. Default: $PWD/.swarm if it exists, else ~/.openclaw/.swarm"
      },
      "includeFiles": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Files within swarmDir to inject into the memory prompt (default: [\"state.md\", \"queue.md\"])"
      },
      "maxBytes": {
        "type": "number",
        "description": "Total byte cap across all injected files. Default 32768. If exceeded, files are truncated with a marker."
      },
      "registerSwarmReadTool": {
        "type": "boolean",
        "description": "If true, also register a swarm_read agent tool so the agent can re-read files mid-session (default: true)"
      }
    }
  }
}
