{
  "name": "@oasis-claw/prompt-injection-reporting",
  "version": "0.1.0",
  "description": "openclaw plugin: agent-callable report_injection tool + signed JSONL attack log + Telegram alerting",
  "type": "module",
  "devDependencies": {
    "openclaw": "workspace:*",
    "vitest": "^3.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
