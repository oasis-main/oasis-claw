{
  "name": "@oasis-claw/approval-gate",
  "version": "0.1.0",
  "description": "openclaw plugin: human-in-the-loop approval surface (CAPTCHA forwarding, API approval policy, browser-approval configuration)",
  "type": "module",
  "devDependencies": {
    "openclaw": "workspace:*",
    "vitest": "^3.0.0"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "zod": "^4.4.1"
  }
}