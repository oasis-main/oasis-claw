{
  "name": "@oasis-claw/agent-primitives",
  "version": "0.1.0",
  "description": "openclaw plugin: sleep / dream / compact agent lifecycle tools",
  "type": "module",
  "devDependencies": {
    "openclaw": "workspace:*",
    "vitest": "^3.0.0"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "zod": "^4.4.1"
  }
}