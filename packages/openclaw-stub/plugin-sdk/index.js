// Runtime stub — all extension imports from this package are `import type`,
// so nothing is actually imported at runtime. This file exists only so Node
// and vitest can resolve the module path without erroring.
export {};
